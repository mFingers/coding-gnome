<p>
  Turns left: <%= @tally.turns_left %>
</p>

<p>
  Word so far: <%= join(@tally.letters) %>
</p>

<%= form_for(@conn, hangman_path(@conn, :make_move), [ as: :make_move, method: :put], fn f -> %>
  <%= text_input(f, :guess) %>
  <%= submit("Make Move") %>
<% end) %>
