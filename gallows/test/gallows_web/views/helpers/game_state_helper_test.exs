defmodule GallowsWeb.GameStateHelperTest do
  use GallowsWeb.ConnCase, async: true

  import Gallows.Views.Helpers.GameStateHelper

  # test "it outputs lost" do
  #   output = game_state({:lost, "wibble"})
  #   assert output =~ "You Lost! the word is wibble"
  # end
end
